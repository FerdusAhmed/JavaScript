const name = "Metropolitan University";
let StudentCount = 3000;
let FacultyCount = 50;
console.log(`welcome to ${name} and university has ${StudentCount} students and ${FacultyCount} faculties`);//using tanplate literals in mordern style

const input = "200"
const age = Number(input);
const secondinput = "true"
const isEnrolled = Boolean(secondinput);
console.log(`the course fee is ${age} and the enrollenment is ${isEnrolled}`);
